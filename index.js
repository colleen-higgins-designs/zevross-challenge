// g
// get wind turbines within contiguous us

// get list of states or map through locations/turbines array and add a marker for each state with turbine (if not already added)

// add marker
// var marker = new mapboxgl.Marker().setLngLat([30.5, 50.5]).addTo(map);
